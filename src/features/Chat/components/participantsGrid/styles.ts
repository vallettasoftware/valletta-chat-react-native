import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  activeSpeakerBorder: {
    borderColor: '#fff000',
    borderRadius: 12,
    borderWidth: 2,
    flex: 1,
    marginBottom: 6,
    marginHorizontal: 12,
  },
  flexContainer: {
    flex: 1,
  },
  messagesList: {
    paddingHorizontal: 10,
  },
  rowContainer: {
    flex: 1,
    flexDirection: 'row',
  },
});

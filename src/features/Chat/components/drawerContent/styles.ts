import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  flex: { flex: 1 },
  footer: {
    borderColor: '#596A7B',
    borderTopWidth: 1,
    color: '#596A7B',
    flexDirection: 'row',
    height: 58,
    lineHeight: 58,
    textAlign: 'center',
  },
});

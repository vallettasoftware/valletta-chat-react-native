import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  avatarContainer: {
    backgroundColor: '#70adf0',
    borderRadius: 50,
    height: '100%',
    width: '58%',
  },
  centerTextContainer: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center',
  },
  roundedContainer: {
    alignItems: 'center',
    borderRadius: 50,
    flex: 1,
    justifyContent: 'center',
  },
  textAvatar: {
    color: '#fff',
    textAlign: 'center',
  },
});

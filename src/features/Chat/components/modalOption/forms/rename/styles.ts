import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  input: {
    backgroundColor: '#fff',
    borderRadius: 4,
    color: '#000',
    fontSize: 18,
    height: 60,
    lineHeight: 22,
    paddingHorizontal: 16,
  },
});
